<h1>Nuevo Pedido</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.medicamento_path(@conn, :create)) %>

<%= link "Volver", to: Routes.medicamento_path(@conn, :index), class: "btn btn-secondary" %>

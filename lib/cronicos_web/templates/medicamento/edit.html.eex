<h1>Editar Medicamento</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.medicamento_path(@conn, :update, @medicamento)) %>

<span><%= link "Volver", to: Routes.medicamento_path(@conn, :index), class: "btn btn-secondary" %></span>
